---
description: ARG academic-paper outline-only mode — detailed outline + evidence map
---

Trigger the `academic-paper` skill in `outline-only` mode. Produces a detailed paper outline with evidence map, no full draft. Balanced spectrum, high oversight.

1. Read the skill entry at `academic-paper/SKILL.md` and locate the `outline-only` mode section.
2. Read `MODE_REGISTRY.md` § academic-paper for mode triggers and output spec.
3. Produce a detailed hierarchical paper outline:
   - Section and subsection structure
   - Key arguments per section
   - Evidence mapping (which sources support which sections)
   - Estimated word counts per section
4. Do NOT produce a full draft — outline and evidence map only.
5. Follow all key rules from the skill: citations required, evidence hierarchy respected, AI disclosure included.
